{
  "auditVersion": 1,
  "name": "security",
  "category": "security",
  "description": "Optional security audit: DOM HTML sinks, dynamic code execution, Subresource Integrity on remote assets and target=_blank link safety. This is not a security assessment and never affects the exit code; run it only with --profile security.",
  "rules": ["UE-SE001", "UE-SE002", "UE-SE003", "UE-SE004"]
}
